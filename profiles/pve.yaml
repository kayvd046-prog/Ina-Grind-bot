name: pve
mode: pve
templates_subdir: pve
match_threshold: 0.85
phase2_enabled: false
button_map:
  confirm: A
  cancel: B
  commander_toggle: Y
  menu: START
timings:
  poll_interval: 0.4
  tap_cooldown: 0.6
  stuck_seconds: 25
  recovery_backoff: 2.0
